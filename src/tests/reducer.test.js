import {reducer} from '../Store';


/**
const reducer = (state = initState , action) =>{
let newState = {...state}
  switch(action.type){
     case 'ADD-TO-CART':
      newState.cart = action.payload.cart + 1;
     break;
     case 'CHANGE-CATEGORY':
       newState.currentCategory = action.payload;
       break;
       default:
         break;
  }
  return newState;
} 
 */


 describe('',() => {
   it('' , () => {
     expect(true).toBe(true);
   })
 })


